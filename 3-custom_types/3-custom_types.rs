// rust custom data types are formed mainly through the two keywords:
// struct:  // define a structure
// enum:    // define an enumeration
// constants can also be created via teh const and static keywords
